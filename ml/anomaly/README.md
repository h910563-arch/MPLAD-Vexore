# ml/anomaly

Reference notes for the four anomaly detectors. The runnable implementation is
`backend/src/services/riskEngine.ts` (functions `assessCost`, `assessDelay`,
`assessExpenditure`, `assessSimilarity` / `findSimilarProjects`). Full method
description: `docs/anomaly-detection.md`.
