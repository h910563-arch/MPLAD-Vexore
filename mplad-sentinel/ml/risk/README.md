# ml/risk

Reference notes for combining anomaly signals into the Prototype Risk Score.
The runnable implementation is `computeRisk` in
`backend/src/services/riskEngine.ts`. Full method description:
`docs/anomaly-detection.md`.
